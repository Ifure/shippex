import {StyleSheet} from 'react-native';
import {SIZES} from '../../theme/size';

export const styles = StyleSheet.create({
  icon_focused: {borderRadius: SIZES.borderRadii.m, padding: SIZES.small},
});
