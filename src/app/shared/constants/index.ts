import {IObjectMapper} from '../types';

export const TAB_ICON: IObjectMapper = {
  Home: 'show-chart',
  Exchange: 'md-swap-horizontal-sharp',
  Transaction: 'show-chart',
  Settings: 'md-time-outline',
};

export const IonicRoutes = ['Exchange', 'Settings'];
