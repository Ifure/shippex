import {StyleSheet} from 'react-native';
import {SPACING} from '../../app/theme/spacing';
import {pixelSizeHorizontal} from '../../app/theme/config';

export const styles = StyleSheet.create({
  header: {
    justifyContent: 'space-between',
    alignItems: 'center',
    flexDirection: 'row',
    paddingVertical: 10,
    zIndex: 1,
  },
});
